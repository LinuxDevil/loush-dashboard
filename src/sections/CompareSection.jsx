// STUB. Owned by the `compare` agent; brief in docs/odysseus-port/2.md.
// Already registered in src/App.jsx and its CSS already imported, so the agent touches only
// this file, server/compare.mjs, and src/styles/compare.css.
import React from 'react'

export default function CompareSection() {
  return <div className="dim" style={{ padding: 16 }}>Compare is not built yet — see docs/odysseus-port/2.md</div>
}
