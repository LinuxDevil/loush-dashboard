// STUB. Owned by the `documents` agent; brief in docs/odysseus-port/4.md.
// Already registered in src/App.jsx and its CSS already imported, so the agent touches only
// this file, server/docs.mjs, and src/styles/docs.css.
import React from 'react'

export default function DocsSection() {
  return <div className="dim" style={{ padding: 16 }}>Documents is not built yet — see docs/odysseus-port/4.md</div>
}
