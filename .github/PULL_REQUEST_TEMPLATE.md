## Summary
_What does this PR do, in a sentence or two?_

## What changed
-

## How it was tested
-

## Checklist
- [ ] `npm test` passes
- [ ] No raw hex colors added in `src/` — "Every colour in the app resolves through the token block at the top of `src/styles.css`... A raw hex anywhere in `src/` is a bug: it breaks the light theme."
