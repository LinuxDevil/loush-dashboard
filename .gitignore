node_modules
atoms/index.json
atoms/reviewed.json

# Engineering Metrics dashboard JIRA creds (never commit)
.eng.local.json
config.json
projects.json
bug-ownership.json
promptcheck.json

# runtime state written to the repo root by server/eng.mjs — your triage decisions, epic targets
# and artifact links, not shipped config. bug-ownership.json above is the same class.
eng-triage.json
eng-epic-targets.json
eng-artifacts.json
.playwright-mcp

# build output — regenerate with `npx vite build`
dist/
docs/
.serena/
.claude/

